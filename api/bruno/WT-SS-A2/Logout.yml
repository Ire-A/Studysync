info:
  name: Logout
  type: http
  seq: 15

http:
  method: POST
  url: "{{baseUrl}}/api/users/logout"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:9000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
