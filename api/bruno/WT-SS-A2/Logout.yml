info:
  name: Logout
  type: http
  seq: 15

http:
  method: POST
  url: "{{render}}/api/users/logout"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: https://studysync-iqaq.onrender.com

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
