info:
  name: Create group
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/groups"
  body:
    type: json
    data: |-
      {
        "name": "Web Technologies",
        "description": "Study group for Web Technologies module"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
