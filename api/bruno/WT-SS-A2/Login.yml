info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{render}}/api/users/login"
  body:
    type: json
    data: |-
      {
        "email": "alex@example.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
