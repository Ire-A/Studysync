info:
  name: Delete User
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{render}}/api/users/profile"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:9000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
